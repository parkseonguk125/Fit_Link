# Database (Docker PostgreSQL)
DATABASE_URL="postgresql://exercise:exercise@localhost:5432/exercise_log?schema=public"

# NextAuth
AUTH_SECRET="local-dev-secret-change-me-in-production"

# 로컬 dev(npm run dev): http://localhost:3000
# Docker 3-tier(npm run docker:up): http://localhost:8081
AUTH_URL="http://localhost:3000"

# Local file uploads
UPLOAD_DIR="./uploads"

# App
NODE_ENV="development"
